[project]
name = "image-classifier"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "uvicorn",
  "numpy",
  "Pillow",
  "python-multipart",
]

[project.scripts]
app = "server:app"
